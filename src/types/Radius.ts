export type Radius = {
    type: "CONSTANT";
    value: string;
    estimated_value: string;
    computed_value: string;
    range: {
        min: string;
        max: string;
    };
    unit: string;
};
